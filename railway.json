{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile",
    "args": {
      "TMDB_API_KEY": "${{TMDB_API_KEY}}",
      "SECRET_KEY": "${{SECRET_KEY}}" 
    }
  }
}